<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <title>Ticket‑Bot Panel</title>
  <link rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
</head>
<body class="container">
  <h1>🎫 Ticket‑Kategorien</h1>

  <form method="POST">
    <table>
      <thead><tr><th>Label</th><th>Value</th><th>Emoji</th></tr></thead>
      <tbody id="rows">
        <% cfg.topics.forEach(t => { %>
        <tr>
          <td><input name="label" value="<%= t.label %>" required></td>
          <td><input name="value" value="<%= t.value %>"></td>
          <td><input name="emoji" value="<%= t.emoji %>"></td>
        </tr>
        <% }) %>
      </tbody>
    </table>

    <button type="button" onclick="addRow()">➕ Neue Zeile</button>
    <button type="submit">💾 Speichern</button>
  </form>
<h2>Panel‑Nachricht</h2>
<form action="/panel/send" method="POST">
  <input name="channelId" placeholder="Channel‑ID" value="<%= cfg.panelChannelId || '' %>" required>
  <button>Senden</button>
</form>

<form action="/panel/edit" method="POST" style="margin-top:1rem">
  <button>Bearbeiten&nbsp;(bestehende Panel‑Nachricht)</button>
</form>


  <script>
    function addRow () {
      const r = document.createElement('tr');
      r.innerHTML = `
        <td><input name="label" required></td>
        <td><input name="value"></td>
        <td><input name="emoji"></td>`;
      document.getElementById('rows').appendChild(r);
    }
  </script>
</body>
</html>
