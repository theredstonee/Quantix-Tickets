<!doctype html>
<html lang="<%= lang || 'de' %>">
<head>
  <!-- Theme Manager - Load before anything else -->
  <script src="/js/theme-manager.js"></script>
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-SLMX16ZJ6Y"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-SLMX16ZJ6Y');
  </script>

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
  <title><%= t.web.title || 'Quantix Tickets - Admin Panel' %></title>

  <!-- Favicon -->
  <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon.png">
  <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon.png">
  <link rel="apple-touch-icon" sizes="180x180" href="/images/favicon.png">
  <link rel="shortcut icon" href="/images/favicon.png">

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <link rel="stylesheet" href="/markdown.css">

  <style>
    /* ============================================================
     * MODERN PANEL DESIGN - COMPLETE REDESIGN
     * ============================================================ */

    /* CSS Variables */
    :root {
      --bg-color: #f0f8ff;
      --text-color: #1a1a2e;
      --border-color: #bdd7ee;
      --accent-color: #0ea5e9;
      --accent-hover: #0284c7;
      --glass-bg: rgba(255, 255, 255, 0.7);
      --glass-border: rgba(14, 165, 233, 0.3);
      --shadow-color: rgba(14, 165, 233, 0.15);
      --success-color: #10b981;
      --warning-color: #f59e0b;
      --error-color: #ef4444;
      --sidebar-width: 280px;
      --header-height: 70px;
    }

    [data-theme="dark"] {
      --bg-color: #0a0a0a;
      --text-color: #00ff88;
      --border-color: #1a3a2a;
      --accent-color: #00ff88;
      --accent-hover: #00dd77;
      --glass-bg: rgba(15, 21, 16, 0.7);
      --glass-border: rgba(0, 255, 136, 0.3);
      --shadow-color: rgba(0, 255, 136, 0.2);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: var(--bg-color);
      color: var(--text-color);
      min-height: 100vh;
      position: relative;
      overflow-x: hidden;
    }

    /* Animated Background */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle, var(--accent-color) 1px, transparent 1px);
      background-size: 50px 50px;
      opacity: 0.05;
      animation: bgMove 20s linear infinite;
      pointer-events: none;
      z-index: 0;
    }

    @keyframes bgMove {
      0% { background-position: 0 0; }
      100% { background-position: 50px 50px; }
    }

    /* Layout */
    .app-container {
      display: flex;
      min-height: 100vh;
      position: relative;
      z-index: 1;
    }

    /* Sidebar */
    .sidebar {
      width: var(--sidebar-width);
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      border-right: 2px solid var(--glass-border);
      padding: 2rem 0;
      position: fixed;
      left: 0;
      top: 0;
      height: 100vh;
      overflow-y: auto;
      z-index: 1000;
      transition: all 0.3s;
    }

    .sidebar-header {
      padding: 0 1.5rem 2rem;
      border-bottom: 1px solid var(--border-color);
    }

    .sidebar-logo {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1.5rem;
    }

    .sidebar-logo i {
      font-size: 2rem;
      color: var(--accent-color);
    }

    .sidebar-logo-text h1 {
      font-size: 1.3rem;
      font-weight: 700;
      color: var(--text-color);
      margin: 0;
    }

    .sidebar-logo-text p {
      font-size: 0.8rem;
      opacity: 0.7;
      margin: 0;
    }

    .server-info {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 1rem;
      background: var(--glass-bg);
      border-radius: 12px;
      border: 2px solid var(--glass-border);
    }

    .server-icon {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      background: var(--accent-color);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      color: white;
      font-size: 1.2rem;
    }

    .server-icon img {
      width: 100%;
      height: 100%;
      border-radius: 50%;
      object-fit: cover;
    }

    .server-details h3 {
      font-size: 0.95rem;
      font-weight: 600;
      margin: 0;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .server-details p {
      font-size: 0.75rem;
      opacity: 0.7;
      margin: 0;
    }

    .user-info {
      margin-top: 0.75rem;
      padding-top: 0.75rem;
      border-top: 1px solid var(--border-color);
    }

    .sidebar-nav {
      padding: 1.5rem 0;
    }

    .nav-section {
      margin-bottom: 2rem;
    }

    .nav-section-title {
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      opacity: 0.5;
      padding: 0 1.5rem;
      margin-bottom: 0.5rem;
    }

    .nav-item {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.75rem 1.5rem;
      color: var(--text-color);
      text-decoration: none;
      transition: all 0.2s;
      border-left: 3px solid transparent;
    }

    .nav-item:hover {
      background: rgba(14, 165, 233, 0.1);
      border-left-color: var(--accent-color);
    }

    .nav-item.active {
      background: rgba(14, 165, 233, 0.15);
      border-left-color: var(--accent-color);
      font-weight: 600;
    }

    .nav-item i {
      font-size: 1.1rem;
      width: 20px;
      text-align: center;
    }

    /* Main Content */
    .main-content {
      margin-left: var(--sidebar-width);
      flex: 1;
      padding: 2rem;
      max-width: calc(100% - var(--sidebar-width));
    }

    .content-header {
      margin-bottom: 2rem;
    }

    .content-title {
      font-size: 2rem;
      font-weight: 700;
      color: var(--text-color);
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .content-title i {
      color: var(--accent-color);
    }

    /* Alerts */
    .alert {
      padding: 1.25rem 1.5rem;
      border-radius: 12px;
      margin-bottom: 1.5rem;
      display: flex;
      align-items: center;
      gap: 1rem;
      animation: slideIn 0.3s ease-out;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .alert-success {
      background: rgba(16, 185, 129, 0.1);
      border: 2px solid var(--success-color);
      color: var(--success-color);
    }

    .alert-warning {
      background: rgba(245, 158, 11, 0.1);
      border: 2px solid var(--warning-color);
      color: var(--warning-color);
    }

    .alert-error {
      background: rgba(239, 68, 68, 0.1);
      border: 2px solid var(--error-color);
      color: var(--error-color);
    }

    .alert i {
      font-size: 1.5rem;
    }

    /* Read-Only Banner */
    .readonly-banner {
      background: linear-gradient(135deg, #f39c12 0%, #e67e22 100%);
      color: white;
      padding: 1.5rem;
      border-radius: 15px;
      text-align: center;
      margin-bottom: 2rem;
      box-shadow: 0 4px 20px rgba(243, 156, 18, 0.4);
    }

    .readonly-banner h3 {
      margin: 0 0 0.5rem 0;
      font-size: 1.5rem;
    }

    .readonly-banner p {
      margin: 0;
      opacity: 0.9;
    }

    /* Accordion Sections */
    .accordion {
      margin-bottom: 1.5rem;
    }

    .accordion-item {
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      border: 2px solid var(--glass-border);
      border-radius: 15px;
      margin-bottom: 1rem;
      overflow: hidden;
      transition: all 0.3s;
    }

    .accordion-item:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 32px var(--shadow-color);
    }

    .accordion-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1.5rem;
      cursor: pointer;
      user-select: none;
      transition: all 0.2s;
    }

    .accordion-header:hover {
      background: rgba(14, 165, 233, 0.05);
    }

    .accordion-title {
      display: flex;
      align-items: center;
      gap: 1rem;
      flex: 1;
    }

    .accordion-icon {
      width: 50px;
      height: 50px;
      background: var(--accent-color);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 1.5rem;
    }

    .accordion-text h3 {
      font-size: 1.2rem;
      font-weight: 600;
      margin: 0 0 0.25rem 0;
      color: var(--text-color);
    }

    .accordion-text p {
      font-size: 0.9rem;
      margin: 0;
      opacity: 0.7;
    }

    .accordion-toggle {
      font-size: 1.5rem;
      color: var(--accent-color);
      transition: transform 0.3s;
    }

    .accordion-item.active .accordion-toggle {
      transform: rotate(180deg);
    }

    .accordion-content {
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.3s ease-out;
    }

    .accordion-item.active .accordion-content {
      max-height: 5000px;
      transition: max-height 0.5s ease-in;
    }

    .accordion-body {
      padding: 0 1.5rem 1.5rem;
    }

    /* Form Elements */
    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-label {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--text-color);
    }

    .form-label i {
      color: var(--accent-color);
    }

    .help-text {
      font-size: 0.85rem;
      opacity: 0.7;
      margin-top: 0.5rem;
      display: flex;
      align-items: flex-start;
      gap: 0.5rem;
    }

    .help-text i {
      margin-top: 0.2rem;
      color: var(--accent-color);
    }

    input, select, textarea {
      width: 100%;
      padding: 0.75rem 1rem;
      border: 2px solid var(--border-color);
      border-radius: 8px;
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      color: var(--text-color);
      font-size: 0.95rem;
      transition: all 0.2s;
    }

    input:focus, select:focus, textarea:focus {
      outline: none;
      border-color: var(--accent-color);
      box-shadow: 0 0 0 3px rgba(14, 165, 233, 0.1);
    }

    input:disabled, select:disabled, textarea:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }

    select[multiple] {
      height: auto;
      min-height: 150px;
    }

    textarea {
      min-height: 120px;
      resize: vertical;
    }

    /* Buttons */
    button, .btn {
      padding: 0.75rem 1.5rem;
      border: none;
      border-radius: 8px;
      font-size: 0.95rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      text-decoration: none;
    }

    .btn-primary {
      background: var(--accent-color);
      color: white;
    }

    .btn-primary:hover {
      background: var(--accent-hover);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(14, 165, 233, 0.3);
    }

    .btn-success {
      background: var(--success-color);
      color: white;
    }

    .btn-success:hover {
      background: #059669;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }

    .btn-warning {
      background: var(--warning-color);
      color: white;
    }

    .btn-outline {
      background: transparent;
      border: 2px solid var(--accent-color);
      color: var(--accent-color);
    }

    .btn-outline:hover {
      background: var(--accent-color);
      color: white;
    }

    .btn-group {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    /* Theme Toggle */
    .theme-toggle {
      position: fixed;
      top: 1rem;
      right: 1rem;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      border: 2px solid var(--glass-border);
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      z-index: 1001;
      transition: all 0.2s;
      box-shadow: 0 4px 12px var(--shadow-color);
    }

    .theme-toggle:hover {
      transform: translateY(-2px) rotate(20deg);
      box-shadow: 0 6px 20px var(--shadow-color);
    }

    .theme-toggle i {
      font-size: 1.2rem;
      color: var(--accent-color);
    }

    /* Language Dropdown */
    .lang-dropdown {
      position: fixed;
      top: 1rem;
      right: 5rem;
      z-index: 1001;
    }

    .lang-dropdown-toggle {
      padding: 0.6rem 1rem;
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      border: 2px solid var(--glass-border);
      border-radius: 25px;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      box-shadow: 0 4px 12px var(--shadow-color);
      transition: all 0.2s;
    }

    .lang-dropdown-toggle:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px var(--shadow-color);
    }

    .lang-dropdown-toggle img {
      width: 24px;
      height: 18px;
      border-radius: 2px;
    }

    .lang-dropdown-menu {
      position: absolute;
      top: calc(100% + 0.5rem);
      right: 0;
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      border: 2px solid var(--glass-border);
      border-radius: 12px;
      box-shadow: 0 8px 24px var(--shadow-color);
      display: none;
      min-width: 200px;
      overflow: hidden;
    }

    .lang-dropdown-menu.show {
      display: block;
      animation: slideDown 0.2s ease-out;
    }

    @keyframes slideDown {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .lang-dropdown-item {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.75rem 1rem;
      cursor: pointer;
      transition: all 0.2s;
      text-decoration: none;
      color: var(--text-color);
      border-bottom: 1px solid var(--border-color);
    }

    .lang-dropdown-item:last-child {
      border-bottom: none;
    }

    .lang-dropdown-item:hover {
      background: var(--accent-color);
      color: white;
    }

    .lang-dropdown-item.active {
      background: var(--accent-color);
      color: white;
      font-weight: 600;
    }

    /* Responsive */
    @media (max-width: 1024px) {
      .sidebar {
        transform: translateX(-100%);
      }

      .sidebar.mobile-open {
        transform: translateX(0);
      }

      .main-content {
        margin-left: 0;
        max-width: 100%;
      }

      .mobile-toggle {
        display: block;
        position: fixed;
        bottom: 2rem;
        left: 2rem;
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: var(--accent-color);
        color: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        z-index: 999;
        box-shadow: 0 4px 20px var(--shadow-color);
      }
    }

    @media (min-width: 1025px) {
      .mobile-toggle {
        display: none;
      }
    }

    /* Loading Overlay */
    .loading-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(5px);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 9999;
    }

    .loading-overlay.show {
      display: flex;
    }

    .loading-spinner {
      width: 60px;
      height: 60px;
      border: 6px solid rgba(14, 165, 233, 0.2);
      border-top-color: var(--accent-color);
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    /* Priority Badge */
    .priority-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
    }

    .priority-green {
      background: rgba(16, 185, 129, 0.2);
      color: var(--success-color);
      border: 1px solid var(--success-color);
    }

    .priority-orange {
      background: rgba(245, 158, 11, 0.2);
      color: var(--warning-color);
      border: 1px solid var(--warning-color);
    }

    .priority-red {
      background: rgba(239, 68, 68, 0.2);
      color: var(--error-color);
      border: 1px solid var(--error-color);
    }

    /* Topics List */
    .topics-list {
      display: grid;
      gap: 1rem;
    }

    .topic-item {
      background: var(--glass-bg);
      padding: 1rem;
      border-radius: 10px;
      border: 2px solid var(--glass-border);
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .topic-emoji {
      font-size: 2rem;
    }

    .topic-details {
      flex: 1;
    }

    .topic-label {
      font-weight: 600;
      margin-bottom: 0.25rem;
    }

    .topic-value {
      font-size: 0.85rem;
      opacity: 0.7;
    }

    /* Save Button Fixed */
    .save-button-fixed {
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      z-index: 998;
      box-shadow: 0 8px 32px rgba(14, 165, 233, 0.4);
    }

    /* Dark Mode Overrides */
    [data-theme="dark"] input,
    [data-theme="dark"] select,
    [data-theme="dark"] textarea {
      background-color: rgba(15, 21, 16, 0.9);
      border-color: var(--border-color);
      color: var(--text-color);
    }

    [data-theme="dark"] .accordion-item {
      background: rgba(15, 21, 16, 0.7);
      border-color: var(--glass-border);
    }

    [data-theme="dark"] .server-info,
    [data-theme="dark"] .topic-item {
      background: rgba(15, 21, 16, 0.7);
      border-color: var(--glass-border);
    }
  </style>
</head>
<body>
  <!-- Loading Overlay -->
  <div class="loading-overlay" id="loadingOverlay">
    <div class="loading-spinner"></div>
  </div>

  <!-- Theme Toggle -->
  <button class="theme-toggle" id="themeToggle" title="<%= lang === 'de' ? 'Dark Mode umschalten' : 'Toggle Dark Mode' %>">
    <i class="fas fa-moon"></i>
  </button>

  <!-- Language Dropdown -->
  <div class="lang-dropdown">
    <button class="lang-dropdown-toggle" onclick="toggleLangDropdown()">
      <%
        const langFlags = { de: 'de', en: 'gb', he: 'il', ja: 'jp', ru: 'ru', pt: 'pt', es: 'es', id: 'id', zh: 'zh', ar: 'ar' };
        const langNames = { de: 'Deutsch', en: 'English', he: 'עברית', ja: '日本語', ru: 'Русский', pt: 'Português', es: 'Español', id: 'Bahasa', zh: '中文', ar: 'العربية' };
      %>
      <img src="/images/flags/<%= langFlags[lang] %>.svg" alt="<%= lang %>">
      <span><%= langNames[lang] %></span>
      <i class="fas fa-chevron-down" style="font-size: 0.7rem;"></i>
    </button>
    <div class="lang-dropdown-menu" id="langDropdownMenu">
      <a href="/set-language/de" class="lang-dropdown-item <%= lang === 'de' ? 'active' : '' %>">
        <img src="/images/flags/de.svg" alt="DE">
        <span>Deutsch</span>
      </a>
      <a href="/set-language/en" class="lang-dropdown-item <%= lang === 'en' ? 'active' : '' %>">
        <img src="/images/flags/gb.svg" alt="EN">
        <span>English</span>
      </a>
      <a href="/set-language/he" class="lang-dropdown-item <%= lang === 'he' ? 'active' : '' %>">
        <img src="/images/flags/il.svg" alt="HE">
        <span>עברית</span>
      </a>
      <a href="/set-language/ja" class="lang-dropdown-item <%= lang === 'ja' ? 'active' : '' %>">
        <img src="/images/flags/jp.svg" alt="JA">
        <span>日本語</span>
      </a>
      <a href="/set-language/ru" class="lang-dropdown-item <%= lang === 'ru' ? 'active' : '' %>">
        <img src="/images/flags/ru.svg" alt="RU">
        <span>Русский</span>
      </a>
      <a href="/set-language/pt" class="lang-dropdown-item <%= lang === 'pt' ? 'active' : '' %>">
        <img src="/images/flags/pt.svg" alt="PT">
        <span>Português</span>
      </a>
      <a href="/set-language/es" class="lang-dropdown-item <%= lang === 'es' ? 'active' : '' %>">
        <img src="/images/flags/es.svg" alt="ES">
        <span>Español</span>
      </a>
      <a href="/set-language/id" class="lang-dropdown-item <%= lang === 'id' ? 'active' : '' %>">
        <img src="/images/flags/id.svg" alt="ID">
        <span>Bahasa</span>
      </a>
      <a href="/set-language/zh" class="lang-dropdown-item <%= lang === 'zh' ? 'active' : '' %>">
        <img src="/images/flags/zh.svg" alt="ZH">
        <span>中文</span>
      </a>
      <a href="/set-language/ar" class="lang-dropdown-item <%= lang === 'ar' ? 'active' : '' %>">
        <img src="/images/flags/ar.svg" alt="AR">
        <span>العربية</span>
      </a>
    </div>
  </div>

  <!-- Mobile Menu Toggle -->
  <button class="mobile-toggle" onclick="toggleMobileSidebar()">
    <i class="fas fa-bars"></i>
  </button>

  <div class="app-container">
    <!-- Sidebar -->
    <aside class="sidebar" id="sidebar">
      <div class="sidebar-header">
        <div class="sidebar-logo">
          <i class="fas fa-ticket-alt"></i>
          <div class="sidebar-logo-text">
            <h1>Quantix Tickets</h1>
            <p><%= lang === 'de' ? 'Admin Panel' : 'Admin Panel' %></p>
          </div>
        </div>

        <!-- Server Info -->
        <% if (typeof guild !== 'undefined' && guild) { %>
        <div class="server-info">
          <div class="server-icon">
            <% if (guild.icon) { %>
              <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png?size=64" alt="Server">
            <% } else { %>
              <%= guild.name.charAt(0).toUpperCase() %>
            <% } %>
          </div>
          <div class="server-details">
            <h3><%= guild.name %></h3>
            <p><%= guild.id %></p>
          </div>
        </div>
        <% } %>

        <!-- User Info -->
        <% if (typeof user !== 'undefined' && user) { %>
        <div class="server-info user-info">
          <div class="server-icon">
            <% if (user.avatar) { %>
              <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png?size=64" alt="User">
            <% } else { %>
              <%= user.username ? user.username.charAt(0).toUpperCase() : 'U' %>
            <% } %>
          </div>
          <div class="server-details">
            <h3><%= user.global_name || user.username %></h3>
            <p>@<%= user.username %></p>
          </div>
        </div>
        <% } %>
      </div>

      <!-- Navigation -->
      <nav class="sidebar-nav">
        <div class="nav-section">
          <div class="nav-section-title"><%= lang === 'de' ? 'Haupt' : 'Main' %></div>
          <a href="/" class="nav-item">
            <i class="fas fa-home"></i>
            <span><%= lang === 'de' ? 'Zur Startseite' : 'Back to Home' %></span>
          </a>
          <a href="/panel" class="nav-item active">
            <i class="fas fa-cog"></i>
            <span><%= lang === 'de' ? 'Einstellungen' : 'Settings' %></span>
          </a>
          <a href="/select-server" class="nav-item">
            <i class="fas fa-server"></i>
            <span><%= lang === 'de' ? 'Server wechseln' : 'Change Server' %></span>
          </a>
        </div>

        <div class="nav-section">
          <div class="nav-section-title"><%= lang === 'de' ? 'Tickets' : 'Tickets' %></div>
          <a href="/tickets" class="nav-item">
            <i class="fas fa-history"></i>
            <span><%= lang === 'de' ? 'Ticket-Verlauf' : 'Ticket History' %></span>
          </a>
          <% if (premiumTier === 'pro' || premiumTier === 'beta' || guildId === '1291125037876904026') { %>
          <a href="/analytics" class="nav-item">
            <i class="fas fa-chart-line"></i>
            <span><%= lang === 'de' ? 'Analytics' : 'Analytics' %></span>
          </a>
          <% } %>
        </div>

        <div class="nav-section">
          <div class="nav-section-title"><%= lang === 'de' ? 'Support' : 'Support' %></div>
          <a href="https://discord.com/invite/mnYbnpyyBS" target="_blank" class="nav-item">
            <i class="fab fa-discord"></i>
            <span><%= lang === 'de' ? 'Support Server' : 'Support Server' %></span>
          </a>
          <a href="/premium" class="nav-item">
            <i class="fas fa-gem"></i>
            <span>Premium</span>
          </a>
          <a href="/logout" class="nav-item">
            <i class="fas fa-sign-out-alt"></i>
            <span><%= lang === 'de' ? 'Abmelden' : 'Logout' %></span>
          </a>
        </div>
      </nav>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
      <div class="content-header">
        <h1 class="content-title">
          <i class="fas fa-cog"></i>
          <%= lang === 'de' ? 'Server-Einstellungen' : 'Server Settings' %>
        </h1>
      </div>

      <!-- Read-Only Mode Notice -->
      <% if (typeof isAdmin !== 'undefined' && !isAdmin) { %>
      <div class="readonly-banner">
        <span style="font-size: 2.5rem;">👁️</span>
        <h3><%= lang === 'de' ? 'Nur-Lese-Modus' : 'Read-Only Mode' %></h3>
        <p>
          <%= lang === 'de'
            ? 'Du kannst als Team-Mitglied alle Einstellungen sehen, aber keine Änderungen vornehmen. Nur Administratoren können Einstellungen bearbeiten.'
            : 'As a team member, you can view all settings but cannot make changes. Only administrators can edit settings.'
          %>
        </p>
      </div>
      <% } %>

      <!-- Success/Error Messages -->
      <% if (msg === 'saved') { %>
      <div class="alert alert-success">
        <i class="fas fa-check-circle"></i>
        <span><%= t.web.messages.saved %></span>
      </div>
      <% } %>
      <% if (msg === 'sent') { %>
      <div class="alert alert-success">
        <i class="fas fa-paper-plane"></i>
        <span><%= t.web.messages.sent %></span>
      </div>
      <% } %>
      <% if (msg === 'edited') { %>
      <div class="alert alert-success">
        <i class="fas fa-edit"></i>
        <span><%= t.web.messages.edited %></span>
      </div>
      <% } %>
      <% if (msg === 'nopanel') { %>
      <div class="alert alert-warning">
        <i class="fas fa-exclamation-triangle"></i>
        <span><%= t.web.messages.nopanel %></span>
      </div>
      <% } %>
      <% if (msg === 'error') { %>
      <div class="alert alert-error">
        <i class="fas fa-times-circle"></i>
        <span><%= t.web.messages.error %></span>
      </div>
      <% } %>

      <!-- Main Form -->
      <form method="POST" action="/panel" autocomplete="off" id="panelForm">
        <div class="accordion">

          <!-- Section 1: Kanäle & Kategorien -->
          <div class="accordion-item active">
            <div class="accordion-header" onclick="toggleAccordion(this)">
              <div class="accordion-title">
                <div class="accordion-icon">
                  <i class="fas fa-hashtag"></i>
                </div>
                <div class="accordion-text">
                  <h3><%= t.web.panel_config.section_channels %></h3>
                  <p><%= t.web.panel_config.section_channels_desc %></p>
                </div>
              </div>
              <i class="fas fa-chevron-down accordion-toggle"></i>
            </div>
            <div class="accordion-content">
              <div class="accordion-body">
                <!-- Guild ID (Read-only) -->
                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-server"></i>
                    <span><%= t.web.guild_id %></span>
                  </div>
                  <input
                    type="text"
                    name="guildId"
                    value="<%= cfg.guildId || guildId || '' %>"
                    readonly
                    style="cursor: not-allowed; opacity: 0.7;">
                  <div class="help-text">
                    <i class="fas fa-info-circle"></i>
                    <span><%= t.web.panel_config.guild_id_help %></span>
                  </div>
                </div>

                <!-- Ticket Category -->
                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-folder-open"></i>
                    <span><%= t.web.ticket_category %></span>
                  </div>
                  <select name="ticketCategoryId" multiple size="5">
                    <%
                    const selectedCategories = Array.isArray(cfg.ticketCategoryId) ? cfg.ticketCategoryId : (cfg.ticketCategoryId ? [cfg.ticketCategoryId] : []);
                    channels.filter(ch => ch.type === 4).forEach(ch => { %>
                      <option value="<%= ch.id %>" <%= selectedCategories.includes(ch.id) ? 'selected' : '' %>>
                        🎫 <%= ch.name %>
                      </option>
                    <% }) %>
                  </select>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.web.panel_config.ticket_category_help %></span>
                  </div>
                </div>

                <!-- Log Channel -->
                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-clipboard-list"></i>
                    <span><%= t.web.log_channel %></span>
                  </div>
                  <select name="logChannelId" multiple size="5">
                    <%
                    const selectedLogChannels = Array.isArray(cfg.logChannelId) ? cfg.logChannelId : (cfg.logChannelId ? [cfg.logChannelId] : []);
                    channels.filter(ch => ch.type === 0).forEach(ch => { %>
                      <option value="<%= ch.id %>" <%= selectedLogChannels.includes(ch.id) ? 'selected' : '' %>>
                        📊 # <%= ch.name %>
                      </option>
                    <% }) %>
                  </select>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.web.panel_config.log_channel_help %></span>
                  </div>
                </div>

                <!-- Transcript Channel -->
                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-file-alt"></i>
                    <span><%= t.web.transcript_channel %></span>
                  </div>
                  <select name="transcriptChannelId" multiple size="5">
                    <%
                    const selectedTranscriptChannels = Array.isArray(cfg.transcriptChannelId) ? cfg.transcriptChannelId : (cfg.transcriptChannelId ? [cfg.transcriptChannelId] : []);
                    channels.filter(ch => ch.type === 0).forEach(ch => { %>
                      <option value="<%= ch.id %>" <%= selectedTranscriptChannels.includes(ch.id) ? 'selected' : '' %>>
                        📄 # <%= ch.name %>
                      </option>
                    <% }) %>
                  </select>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.web.panel_config.transcript_channel_help %></span>
                  </div>
                </div>

                <!-- GitHub Webhook Channel -->
                <div class="form-group">
                  <div class="form-label">
                    <i class="fab fa-github"></i>
                    <span><%= t.web.panel_config.github_webhook_channel %></span>
                  </div>
                  <select name="githubWebhookChannelId">
                    <option value=""><%= t.web.select_channel %></option>
                    <%
                    channels.filter(ch => ch.type === 0).forEach(ch => { %>
                      <option value="<%= ch.id %>" <%= cfg.githubWebhookChannelId === ch.id ? 'selected' : '' %>>
                        # <%= ch.name %>
                      </option>
                    <% }) %>
                  </select>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.web.panel_config.github_webhook_channel_help %></span>
                  </div>
                </div>

                <div class="alert alert-warning" style="margin-top: 1.5rem;">
                  <i class="fas fa-info-circle"></i>
                  <span><%= t.web.panel_config.multiselect_hint %></span>
                </div>
              </div>
            </div>
          </div>

          <!-- Section 2: Team & Rollen -->
          <div class="accordion-item">
            <div class="accordion-header" onclick="toggleAccordion(this)">
              <div class="accordion-title">
                <div class="accordion-icon">
                  <i class="fas fa-users-cog"></i>
                </div>
                <div class="accordion-text">
                  <h3><%= t.web.panel_config.section_team %></h3>
                  <p><%= t.web.panel_config.section_team_desc %></p>
                </div>
              </div>
              <i class="fas fa-chevron-down accordion-toggle"></i>
            </div>
            <div class="accordion-content">
              <div class="accordion-body">
                <!-- Priority Roles Info -->
                <div class="alert alert-warning">
                  <i class="fas fa-info-circle"></i>
                  <span><%= t.web.panel_config.priority_roles_help %></span>
                </div>

                <!-- Green Priority Roles -->
                <div class="form-group">
                  <div class="form-label">
                    <span class="priority-badge priority-green">
                      <span>🟢</span>
                      <%= lang === 'de' ? 'Grün (Niedrig)' : 'Green (Low)' %>
                    </span>
                  </div>
                  <select name="priorityRoles_0" multiple size="5">
                    <%
                    const green = (cfg.priorityRoles && cfg.priorityRoles['0']) || [];
                    roles.forEach(role => { %>
                      <option value="<%= role.id %>" <%= green.includes(role.id) ? 'selected' : '' %>>
                        @ <%= role.name %>
                      </option>
                    <% }) %>
                  </select>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.web.panel_config.priority_green_help %></span>
                  </div>
                </div>

                <!-- Orange Priority Roles -->
                <div class="form-group">
                  <div class="form-label">
                    <span class="priority-badge priority-orange">
                      <span>🟠</span>
                      <%= lang === 'de' ? 'Orange (Mittel)' : 'Orange (Medium)' %>
                    </span>
                  </div>
                  <select name="priorityRoles_1" multiple size="5">
                    <%
                    const orange = (cfg.priorityRoles && cfg.priorityRoles['1']) || [];
                    roles.forEach(role => { %>
                      <option value="<%= role.id %>" <%= orange.includes(role.id) ? 'selected' : '' %>>
                        @ <%= role.name %>
                      </option>
                    <% }) %>
                  </select>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.web.panel_config.priority_orange_help %></span>
                  </div>
                </div>

                <!-- Red Priority Roles -->
                <div class="form-group">
                  <div class="form-label">
                    <span class="priority-badge priority-red">
                      <span>🔴</span>
                      <%= lang === 'de' ? 'Rot (Hoch)' : 'Red (High)' %>
                    </span>
                  </div>
                  <select name="priorityRoles_2" multiple size="5">
                    <%
                    const red = (cfg.priorityRoles && cfg.priorityRoles['2']) || [];
                    roles.forEach(role => { %>
                      <option value="<%= role.id %>" <%= red.includes(role.id) ? 'selected' : '' %>>
                        @ <%= role.name %>
                      </option>
                    <% }) %>
                  </select>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.web.panel_config.priority_red_help %></span>
                  </div>
                </div>

                <!-- Legacy Team Role -->
                <div class="form-group" style="opacity: 0.7;">
                  <div class="form-label">
                    <i class="fas fa-users"></i>
                    <span><%= t.web.team_role %> (Legacy)</span>
                  </div>
                  <select name="teamRoleId" multiple size="5">
                    <%
                    const selectedTeamRoles = Array.isArray(cfg.teamRoleId) ? cfg.teamRoleId : (cfg.teamRoleId ? [cfg.teamRoleId] : []);
                    roles.forEach(role => { %>
                      <option value="<%= role.id %>" <%= selectedTeamRoles.includes(role.id) ? 'selected' : '' %>>
                        🧑‍💼 @ <%= role.name %>
                      </option>
                    <% }) %>
                  </select>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.web.panel_config.team_role_help %></span>
                  </div>
                </div>

                <!-- Role Summary -->
                <div class="alert alert-success" style="margin-top: 1.5rem;">
                  <i class="fas fa-chart-pie"></i>
                  <div>
                    <strong><%= lang === 'de' ? '📊 Zusammenfassung:' : '📊 Summary:' %></strong><br>
                    <span style="font-size: 0.9rem;">
                      🟢 <%= lang === 'de' ? 'Grün' : 'Green' %>: <%= green.length %> <%= lang === 'de' ? 'Rolle(n)' : 'Role(s)' %> |
                      🟠 <%= lang === 'de' ? 'Orange' : 'Orange' %>: <%= orange.length %> <%= lang === 'de' ? 'Rolle(n)' : 'Role(s)' %> |
                      🔴 <%= lang === 'de' ? 'Rot' : 'Red' %>: <%= red.length %> <%= lang === 'de' ? 'Rolle(n)' : 'Role(s)' %>
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Section 3: Ticket-Themen -->
          <div class="accordion-item">
            <div class="accordion-header" onclick="toggleAccordion(this)">
              <div class="accordion-title">
                <div class="accordion-icon">
                  <i class="fas fa-tags"></i>
                </div>
                <div class="accordion-text">
                  <h3><%= t.web.panel_config.section_topics %></h3>
                  <p><%= t.web.panel_config.section_topics_desc %></p>
                </div>
              </div>
              <i class="fas fa-chevron-down accordion-toggle"></i>
            </div>
            <div class="accordion-content">
              <div class="accordion-body">
                <div class="help-text" style="margin-bottom: 1.5rem;">
                  <i class="fas fa-info-circle"></i>
                  <span><%= t.web.panel_config.topics_help %></span>
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-code"></i>
                    <span><%= t.web.topics_json %></span>
                  </div>
                  <textarea
                    name="topicsJson"
                    rows="12"
                    placeholder='[{"label": "Support", "value": "support", "emoji": "❓"}]'
                  ><%= JSON.stringify(cfg.topics || [], null, 2) %></textarea>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span>
                      <%= lang === 'de'
                        ? 'JSON-Format: Array von Objekten mit "label", "value" und "emoji".'
                        : 'JSON format: Array of objects with "label", "value" and "emoji".'
                      %>
                    </span>
                  </div>
                </div>

                <!-- Topics Preview -->
                <% if (cfg.topics && cfg.topics.length > 0) { %>
                <div style="margin-top: 1.5rem;">
                  <strong><%= lang === 'de' ? '📋 Vorschau:' : '📋 Preview:' %></strong>
                  <div class="topics-list" style="margin-top: 1rem;">
                    <% cfg.topics.forEach(topic => { %>
                    <div class="topic-item">
                      <div class="topic-emoji"><%= topic.emoji || '📌' %></div>
                      <div class="topic-details">
                        <div class="topic-label"><%= topic.label %></div>
                        <div class="topic-value"><%= topic.value %></div>
                      </div>
                    </div>
                    <% }) %>
                  </div>
                </div>
                <% } %>
              </div>
            </div>
          </div>

          <!-- Section 4: Formular-Felder -->
          <div class="accordion-item">
            <div class="accordion-header" onclick="toggleAccordion(this)">
              <div class="accordion-title">
                <div class="accordion-icon">
                  <i class="fas fa-clipboard-question"></i>
                </div>
                <div class="accordion-text">
                  <h3><%= t.web.panel_config.section_forms %></h3>
                  <p><%= t.web.panel_config.section_forms_desc %></p>
                </div>
              </div>
              <i class="fas fa-chevron-down accordion-toggle"></i>
            </div>
            <div class="accordion-content">
              <div class="accordion-body">
                <div class="help-text" style="margin-bottom: 1.5rem;">
                  <i class="fas fa-info-circle"></i>
                  <span><%= t.web.panel_config.forms_help %></span>
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-code"></i>
                    <span><%= t.web.form_fields_json %></span>
                  </div>
                  <textarea
                    name="formFieldsJson"
                    rows="12"
                    placeholder='[{"label": "Beschreibung", "placeholder": "Beschreibe dein Problem..."}]'
                  ><%= JSON.stringify(cfg.formFields || [], null, 2) %></textarea>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span>
                      <%= lang === 'de'
                        ? 'JSON-Format: Array von Objekten mit "label" und "placeholder".'
                        : 'JSON format: Array of objects with "label" and "placeholder".'
                      %>
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Section 5: Nachrichten-Design -->
          <div class="accordion-item">
            <div class="accordion-header" onclick="toggleAccordion(this)">
              <div class="accordion-title">
                <div class="accordion-icon">
                  <i class="fas fa-palette"></i>
                </div>
                <div class="accordion-text">
                  <h3><%= t.web.panel_config.section_embeds %></h3>
                  <p><%= t.web.panel_config.section_embeds_desc %></p>
                </div>
              </div>
              <i class="fas fa-chevron-down accordion-toggle"></i>
            </div>
            <div class="accordion-content">
              <div class="accordion-body">
                <!-- Ticket Embed -->
                <h4 style="margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
                  <i class="fas fa-ticket-alt" style="color: var(--accent-color);"></i>
                  <%= t.web.ticket_embed %>
                </h4>
                <div class="help-text" style="margin-bottom: 1rem;">
                  <i class="fas fa-info-circle"></i>
                  <span><%= t.web.panel_config.ticket_embed_help %></span>
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-heading"></i>
                    <span><%= t.web.embed_title %></span>
                  </div>
                  <input
                    type="text"
                    name="embedTitle"
                    value="<%= cfg.embedTitle || '' %>"
                    placeholder="<%= lang === 'de' ? 'Ticket #{id}' : 'Ticket #{id}' %>">
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-align-left"></i>
                    <span><%= t.web.embed_description %></span>
                  </div>
                  <textarea
                    name="embedDescription"
                    rows="4"
                    placeholder="<%= lang === 'de' ? 'Hallo {user}, willkommen in deinem Ticket!' : 'Hello {user}, welcome to your ticket!' %>"
                  ><%= cfg.embedDescription || '' %></textarea>
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-palette"></i>
                    <span><%= t.web.embed_color %></span>
                  </div>
                  <input
                    type="text"
                    name="embedColor"
                    value="<%= cfg.embedColor || '#0ea5e9' %>"
                    placeholder="#0ea5e9">
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-shoe-prints"></i>
                    <span><%= t.web.embed_footer %></span>
                  </div>
                  <input
                    type="text"
                    name="embedFooter"
                    value="<%= cfg.embedFooter || '' %>"
                    placeholder="<%= lang === 'de' ? 'Quantix Tickets System' : 'Quantix Tickets System' %>">
                </div>

                <hr style="margin: 2rem 0; border: none; border-top: 1px solid var(--border-color);">

                <!-- Panel Embed -->
                <h4 style="margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
                  <i class="fas fa-rectangle-list" style="color: var(--accent-color);"></i>
                  <%= t.web.panel_embed %>
                </h4>
                <div class="help-text" style="margin-bottom: 1rem;">
                  <i class="fas fa-info-circle"></i>
                  <span><%= t.web.panel_config.panel_embed_help %></span>
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-heading"></i>
                    <span><%= t.web.embed_title %></span>
                  </div>
                  <input
                    type="text"
                    name="panelTitle"
                    value="<%= cfg.panelTitle || '' %>"
                    placeholder="<%= lang === 'de' ? '🎫 Ticket System' : '🎫 Ticket System' %>">
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-align-left"></i>
                    <span><%= t.web.embed_description %></span>
                  </div>
                  <textarea
                    name="panelDescription"
                    rows="4"
                    placeholder="<%= lang === 'de' ? 'Wähle ein Thema aus, um ein Ticket zu erstellen.' : 'Select a topic to create a ticket.' %>"
                  ><%= cfg.panelDescription || '' %></textarea>
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-palette"></i>
                    <span><%= t.web.embed_color %></span>
                  </div>
                  <input
                    type="text"
                    name="panelColor"
                    value="<%= cfg.panelColor || '#0ea5e9' %>"
                    placeholder="#0ea5e9">
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-shoe-prints"></i>
                    <span><%= t.web.embed_footer %></span>
                  </div>
                  <input
                    type="text"
                    name="panelFooter"
                    value="<%= cfg.panelFooter || '' %>"
                    placeholder="<%= lang === 'de' ? 'Klicke auf ein Thema, um ein Ticket zu öffnen' : 'Click a topic to open a ticket' %>">
                </div>

                <!-- Placeholders Info -->
                <div class="alert alert-warning" style="margin-top: 1.5rem;">
                  <i class="fas fa-code"></i>
                  <div>
                    <strong><%= t.web.placeholders %>:</strong><br>
                    <code>{user}</code> = <%= lang === 'de' ? 'Benutzer-Mention' : 'User mention' %><br>
                    <code>{id}</code> = Ticket ID<br>
                    <code>{topic}</code> = <%= lang === 'de' ? 'Ticket-Thema' : 'Ticket topic' %>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Section 6: Panel senden -->
          <div class="accordion-item">
            <div class="accordion-header" onclick="toggleAccordion(this)">
              <div class="accordion-title">
                <div class="accordion-icon">
                  <i class="fas fa-paper-plane"></i>
                </div>
                <div class="accordion-text">
                  <h3><%= t.web.panel_config.section_panel %></h3>
                  <p><%= t.web.panel_config.section_panel_desc %></p>
                </div>
              </div>
              <i class="fas fa-chevron-down accordion-toggle"></i>
            </div>
            <div class="accordion-content">
              <div class="accordion-body">
                <div class="help-text" style="margin-bottom: 1.5rem;">
                  <i class="fas fa-info-circle"></i>
                  <span><%= t.web.panel_config.panel_channel_help %></span>
                </div>

                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-hashtag"></i>
                    <span><%= t.web.panel_channel %></span>
                  </div>
                  <select name="panelChannelId">
                    <option value=""><%= t.web.select_channel %></option>
                    <%
                    channels.filter(ch => ch.type === 0).forEach(ch => { %>
                      <option value="<%= ch.id %>" <%= cfg.panelChannelId === ch.id ? 'selected' : '' %>>
                        # <%= ch.name %>
                      </option>
                    <% }) %>
                  </select>
                </div>

                <div class="alert alert-warning">
                  <i class="fas fa-exclamation-triangle"></i>
                  <span><%= t.web.panel_config.save_first %></span>
                </div>

                <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
                <div class="btn-group" style="margin-top: 1.5rem;">
                  <button
                    type="button"
                    class="btn btn-success"
                    onclick="sendPanel()"
                    <% if (!cfg.panelChannelId) { %>disabled<% } %>>
                    <i class="fas fa-paper-plane"></i>
                    <%= t.web.send_panel %>
                  </button>
                  <button
                    type="button"
                    class="btn btn-warning"
                    onclick="editPanel()"
                    <% if (!cfg.panelMessageId) { %>disabled<% } %>>
                    <i class="fas fa-edit"></i>
                    <%= t.web.edit_panel %>
                  </button>
                </div>
                <% } %>
              </div>
            </div>
          </div>

          <!-- Section 7: Premium Features -->
          <% if (isPremium) { %>
          <div class="accordion-item" style="border-color: rgba(102, 126, 234, 0.5);">
            <div class="accordion-header" onclick="toggleAccordion(this)">
              <div class="accordion-title">
                <div class="accordion-icon" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                  <i class="fas fa-gem"></i>
                </div>
                <div class="accordion-text">
                  <h3>
                    <%= t.web.panel_config.section_premium %>
                    <span class="priority-badge" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; margin-left: 0.5rem;">
                      <%= premiumTierName %>
                    </span>
                  </h3>
                  <p><%= t.web.panel_config.section_premium_desc %></p>
                </div>
              </div>
              <i class="fas fa-chevron-down accordion-toggle"></i>
            </div>
            <div class="accordion-content">
              <div class="accordion-body">
                <!-- Custom Avatar (Basic+) -->
                <% if (premiumTier === 'basic' || premiumTier === 'pro' || premiumTier === 'beta') { %>
                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-image"></i>
                    <span><%= t.premium.custom_avatar_label %></span>
                    <span class="priority-badge" style="background: #667eea; color: white; font-size: 0.7rem;">BASIC+</span>
                  </div>
                  <input
                    type="url"
                    name="customAvatarUrl"
                    value="<%= cfg.customAvatarUrl || '' %>"
                    placeholder="<%= t.premium.custom_avatar_placeholder %>">
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.premium.custom_avatar_description %></span>
                  </div>
                </div>
                <% } %>

                <!-- Email Notifications (Pro) -->
                <% if (premiumTier === 'pro' || premiumTier === 'beta') { %>
                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-envelope"></i>
                    <span><%= t.premium.email_notifications_label %></span>
                    <span class="priority-badge" style="background: #764ba2; color: white; font-size: 0.7rem;">PRO</span>
                  </div>
                  <input
                    type="email"
                    name="notificationEmail"
                    value="<%= cfg.notificationEmail || '' %>"
                    placeholder="<%= t.premium.email_notifications_placeholder %>">
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.premium.email_notifications_description %></span>
                  </div>
                </div>

                <!-- Auto-Close System (Pro) -->
                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-clock"></i>
                    <span><%= t.premium.auto_close_label %></span>
                    <span class="priority-badge" style="background: #764ba2; color: white; font-size: 0.7rem;">PRO</span>
                  </div>
                  <label style="display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0.5rem;">
                    <input
                      type="checkbox"
                      name="autoCloseEnabled"
                      <%= cfg.autoCloseEnabled ? 'checked' : '' %>
                      style="width: auto;">
                    <span><%= t.premium.auto_close_enable %></span>
                  </label>
                  <input
                    type="number"
                    name="autoCloseDays"
                    value="<%= cfg.autoCloseDays || 7 %>"
                    min="1"
                    max="365"
                    placeholder="7">
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.premium.auto_close_description %></span>
                  </div>
                </div>

                <!-- DM Notifications (Pro) -->
                <div class="form-group">
                  <div class="form-label">
                    <i class="fas fa-bell"></i>
                    <span><%= t.premium.dm_notifications_label %></span>
                    <span class="priority-badge" style="background: #764ba2; color: white; font-size: 0.7rem;">PRO</span>
                  </div>
                  <textarea
                    name="dmNotificationUsers"
                    rows="4"
                    placeholder="<%= t.premium.dm_notifications_placeholder %>"
                  ><%= (cfg.dmNotificationUsers || []).join('\n') %></textarea>
                  <div class="help-text">
                    <i class="fas fa-lightbulb"></i>
                    <span><%= t.premium.dm_notifications_description %></span>
                  </div>
                </div>
                <% } %>

                <!-- Link to Premium Page -->
                <div class="btn-group" style="margin-top: 1.5rem;">
                  <a href="/premium" class="btn btn-outline">
                    <i class="fas fa-gem"></i>
                    <%= t.premium.manage_subscription %>
                  </a>
                </div>
              </div>
            </div>
          </div>
          <% } else { %>
          <!-- Premium Upgrade CTA -->
          <div class="accordion-item" style="border-color: rgba(102, 126, 234, 0.3);">
            <div class="accordion-header" onclick="toggleAccordion(this)">
              <div class="accordion-title">
                <div class="accordion-icon" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                  <i class="fas fa-gem"></i>
                </div>
                <div class="accordion-text">
                  <h3><%= t.premium.upgrade_cta_title %></h3>
                  <p><%= t.premium.upgrade_cta_description %></p>
                </div>
              </div>
              <i class="fas fa-chevron-down accordion-toggle"></i>
            </div>
            <div class="accordion-content">
              <div class="accordion-body">
                <div class="alert alert-warning">
                  <i class="fas fa-star"></i>
                  <div>
                    <strong><%= lang === 'de' ? '🌟 Premium Features:' : '🌟 Premium Features:' %></strong><br>
                    ✅ <%= t.premium.feature_custom_avatar %> (Basic)<br>
                    ✅ <%= t.premium.feature_statistics %> (Basic)<br>
                    ✅ <%= t.premium.feature_email_notifications %> (Pro)<br>
                    ✅ <%= t.premium.feature_auto_close %> (Pro)<br>
                    ✅ <%= t.premium.feature_dm_notifications %> (Pro)
                  </div>
                </div>

                <div class="btn-group">
                  <a href="/premium" class="btn btn-primary">
                    <i class="fas fa-gem"></i>
                    <%= t.premium.upgrade_cta_button %>
                  </a>
                </div>
              </div>
            </div>
          </div>
          <% } %>

        </div>

        <!-- Save Button (Fixed) -->
        <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
        <button type="submit" class="btn btn-primary save-button-fixed">
          <i class="fas fa-save"></i>
          <%= t.web.save %>
        </button>
        <% } %>
      </form>
    </main>
  </div>

  <!-- JavaScript -->
  <script src="/js/ux-enhancements.js"></script>
  <script>
    // Accordion Toggle
    function toggleAccordion(header) {
      const item = header.parentElement;
      const wasActive = item.classList.contains('active');

      // Close all accordions
      document.querySelectorAll('.accordion-item').forEach(i => {
        i.classList.remove('active');
      });

      // Open clicked accordion if it wasn't active
      if (!wasActive) {
        item.classList.add('active');
      }
    }

    // Theme is managed by /js/theme-manager.js (Cookie-based)

    // Language Dropdown
    function toggleLangDropdown() {
      const menu = document.getElementById('langDropdownMenu');
      menu.classList.toggle('show');
    }

    // Close dropdown when clicking outside
    document.addEventListener('click', function(e) {
      const dropdown = document.querySelector('.lang-dropdown');
      if (dropdown && !dropdown.contains(e.target)) {
        document.getElementById('langDropdownMenu').classList.remove('show');
      }
    });

    // Mobile Sidebar Toggle
    function toggleMobileSidebar() {
      document.getElementById('sidebar').classList.toggle('mobile-open');
    }

    // Send Panel
    function sendPanel() {
      if (!confirm('<%= lang === "de" ? "Möchtest du die Panel-Nachricht jetzt senden?" : "Do you want to send the panel message now?" %>')) {
        return;
      }

      document.getElementById('loadingOverlay').classList.add('show');

      const form = document.createElement('form');
      form.method = 'POST';
      form.action = '/panel/send';
      document.body.appendChild(form);
      form.submit();
    }

    // Edit Panel
    function editPanel() {
      if (!confirm('<%= lang === "de" ? "Möchtest du die Panel-Nachricht aktualisieren?" : "Do you want to update the panel message?" %>')) {
        return;
      }

      document.getElementById('loadingOverlay').classList.add('show');

      const form = document.createElement('form');
      form.method = 'POST';
      form.action = '/panel/edit';
      document.body.appendChild(form);
      form.submit();
    }

    // Form Submit Loading
    document.getElementById('panelForm').addEventListener('submit', function() {
      document.getElementById('loadingOverlay').classList.add('show');
    });

    // Auto-open first accordion on load
    window.addEventListener('DOMContentLoaded', function() {
      const firstAccordion = document.querySelector('.accordion-item');
      if (firstAccordion && !firstAccordion.classList.contains('active')) {
        firstAccordion.classList.add('active');
      }
    });
  </script>
</body>
</html>
