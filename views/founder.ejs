<!DOCTYPE html>
<html lang="de" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Founder Panel - Quantix Tickets</title>
  <link rel="icon" type="image/png" href="/images/favicon.png">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg-color: #0a0a0a;
      --text-color: #00ff88;
      --border-color: #1a3a2a;
      --accent-color: #00ff88;
      --accent-hover: #00dd77;
      --glass-bg: rgba(15, 21, 16, 0.7);
      --glass-border: rgba(0, 255, 136, 0.3);
      --shadow-color: rgba(0, 255, 136, 0.2);
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: var(--bg-color);
      color: var(--text-color);
      min-height: 100vh;
      padding: 2rem;
      position: relative;
    }

    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
      background: radial-gradient(circle, var(--accent-color) 1px, transparent 1px);
      background-size: 50px 50px;
      opacity: 0.03;
      animation: bgMove 20s linear infinite;
    }

    @keyframes bgMove {
      0% { transform: translate(0, 0); }
      100% { transform: translate(50px, 50px); }
    }

    .container {
      max-width: 1600px;
      margin: 0 auto;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 3rem;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .header h1 {
      font-size: 2.5rem;
      color: var(--accent-color);
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .user-info {
      display: flex;
      align-items: center;
      gap: 1rem;
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      padding: 1rem 1.5rem;
      border-radius: 1rem;
      border: 2px solid var(--glass-border);
    }

    .user-avatar {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      border: 2px solid var(--accent-color);
      flex-shrink: 0;
    }

    .user-info > div {
      flex: 1;
      min-width: 0;
      overflow: hidden;
    }

    .user-info > div > div {
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .section {
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 2px solid var(--glass-border);
      border-radius: 1rem;
      padding: 2rem;
      margin-bottom: 2rem;
      box-shadow: 0 8px 32px var(--shadow-color);
    }

    .section-title {
      font-size: 1.8rem;
      margin-bottom: 2rem;
      color: var(--accent-color);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .table-wrapper {
      max-height: 600px;
      overflow-y: auto;
      border-radius: 0.5rem;
      margin-top: 1.5rem;
    }

    .table-wrapper::-webkit-scrollbar {
      width: 12px;
    }

    .table-wrapper::-webkit-scrollbar-track {
      background: rgba(0, 255, 136, 0.05);
      border-radius: 6px;
    }

    .table-wrapper::-webkit-scrollbar-thumb {
      background: var(--accent-color);
      border-radius: 6px;
      border: 2px solid transparent;
      background-clip: padding-box;
    }

    .table-wrapper::-webkit-scrollbar-thumb:hover {
      background: var(--accent-hover);
      border: 2px solid transparent;
      background-clip: padding-box;
    }

    .guilds-table {
      width: 100%;
      border-collapse: collapse;
    }

    .guilds-table thead {
      background: rgba(0, 255, 136, 0.1);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .guilds-table th {
      padding: 1rem;
      text-align: left;
      font-weight: 600;
      text-transform: uppercase;
      font-size: 0.85rem;
      letter-spacing: 0.05em;
      border-bottom: 2px solid var(--glass-border);
      background: rgba(0, 255, 136, 0.1);
      backdrop-filter: blur(10px);
    }

    .guilds-table td {
      padding: 1rem;
      border-bottom: 1px solid var(--glass-border);
    }

    .guilds-table tr:hover {
      background: rgba(0, 255, 136, 0.05);
    }

    .guild-info {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .guild-icon {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      border: 2px solid var(--glass-border);
      object-fit: cover;
    }

    .guild-icon-placeholder {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      background: var(--accent-color);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      color: #000;
      font-size: 1.2rem;
    }

    .guild-name {
      font-weight: 600;
      font-size: 1.1rem;
    }

    .guild-id {
      font-size: 0.85rem;
      opacity: 0.6;
      font-family: 'Courier New', monospace;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      max-width: 200px;
    }

    .badge {
      display: inline-block;
      padding: 0.4rem 0.8rem;
      border-radius: 0.5rem;
      font-size: 0.85rem;
      font-weight: 600;
      text-transform: uppercase;
    }

    .badge-blocked {
      background: rgba(231, 76, 60, 0.2);
      color: #e74c3c;
    }

    .badge-active {
      background: rgba(0, 255, 136, 0.2);
      color: var(--accent-color);
    }

    .btn {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      background: var(--accent-color);
      color: #000;
      text-decoration: none;
      border-radius: 0.5rem;
      font-weight: 600;
      transition: all 0.3s;
      border: none;
      cursor: pointer;
    }

    .btn:hover {
      background: var(--accent-hover);
      transform: translateY(-2px);
    }

    .btn-secondary {
      background: transparent;
      border: 2px solid var(--accent-color);
      color: var(--accent-color);
    }

    .btn-secondary:hover {
      background: var(--accent-color);
      color: #000;
    }

    .search-box {
      margin-bottom: 1.5rem;
    }

    .search-box input {
      width: 100%;
      padding: 1rem;
      background: rgba(0, 255, 136, 0.05);
      border: 2px solid var(--glass-border);
      border-radius: 0.75rem;
      color: var(--text-color);
      font-size: 1rem;
    }

    .search-box input:focus {
      outline: none;
      border-color: var(--accent-color);
    }

    /* Modal */
    .modal-overlay {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(5px);
      z-index: 9999;
      animation: fadeIn 0.3s ease;
    }

    .modal-overlay.active {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .modal-content {
      background: var(--glass-bg);
      backdrop-filter: blur(20px);
      border: 2px solid var(--glass-border);
      border-radius: 1rem;
      padding: 2.5rem;
      max-width: 500px;
      width: 90%;
      box-shadow: 0 20px 60px var(--shadow-color);
      animation: slideUp 0.3s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes slideUp {
      from { transform: translateY(50px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }

    .modal-icon {
      font-size: 4rem;
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .modal-title {
      font-size: 1.8rem;
      color: var(--accent-color);
      margin-bottom: 1rem;
      text-align: center;
      font-weight: 700;
    }

    .modal-message {
      font-size: 1.1rem;
      text-align: center;
      margin-bottom: 2rem;
      opacity: 0.9;
      line-height: 1.6;
    }

    .modal-buttons {
      display: flex;
      gap: 1rem;
      justify-content: center;
    }

    .modal-btn {
      padding: 0.75rem 2rem;
      border: none;
      border-radius: 0.75rem;
      font-weight: 600;
      font-size: 1rem;
      cursor: pointer;
      transition: all 0.3s;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
    }

    .modal-btn-confirm {
      background: #e74c3c;
      color: #fff;
    }

    .modal-btn-confirm:hover {
      background: #c0392b;
      transform: translateY(-2px);
      box-shadow: 0 8px 16px rgba(231, 76, 60, 0.3);
    }

    .modal-btn-cancel {
      background: transparent;
      border: 2px solid var(--accent-color);
      color: var(--accent-color);
    }

    .modal-btn-cancel:hover {
      background: var(--accent-color);
      color: #000;
      transform: translateY(-2px);
    }

    @media (max-width: 768px) {
      body {
        padding: 1rem;
      }

      .header h1 {
        font-size: 1.8rem;
      }

      .guilds-table {
        display: block;
        overflow-x: auto;
      }

      .modal-content {
        max-width: 95%;
        margin: 1rem;
      }

      .modal-buttons {
        flex-direction: column;
      }

      .modal-btn {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>
        <i class="fas fa-user-shield"></i>
        Founder Panel
      </h1>
      <div class="user-info">
        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar" class="user-avatar" onerror="this.src='https://cdn.discordapp.com/embed/avatars/0.png'">
        <div>
          <div style="font-weight: 600;"><%= user.username %></div>
          <div style="font-size: 0.85rem; opacity: 0.7;">Founder</div>
        </div>
      </div>
    </div>

    <div class="section">
      <h2 class="section-title">
        <i class="fas fa-server"></i>
        Server Management (<%= guilds.length %>)
      </h2>

      <div class="search-box">
        <input type="text" id="searchInput" placeholder="🔍 Server suchen (Name oder ID)...">
      </div>

      <div class="table-wrapper">
        <table class="guilds-table">
          <thead>
            <tr>
              <th>Server</th>
              <th>Mitglieder</th>
              <th>Status</th>
              <% if (!restrictedView) { %>
              <th>Aktionen</th>
              <% } %>
            </tr>
          </thead>
          <tbody id="guildsTable">
            <% guilds.forEach(guild => { %>
            <tr>
            <td>
              <div class="guild-info">
                <% if (guild.icon) { %>
                  <img src="<%= guild.icon %>" alt="<%= guild.name %>" class="guild-icon" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                  <div class="guild-icon-placeholder" style="display: none;"><%= guild.name[0] %></div>
                <% } else { %>
                  <div class="guild-icon-placeholder"><%= guild.name[0] %></div>
                <% } %>
                <div>
                  <div class="guild-name"><%= guild.name %></div>
                  <div class="guild-id"><%= guild.id %></div>
                </div>
              </div>
            </td>
            <td><strong><%= guild.memberCount.toLocaleString() %></strong></td>
            <td>
              <% if (guild.blocked) { %>
                <span class="badge badge-blocked"><i class="fas fa-ban"></i> Blockiert</span>
                <% if (guild.blockReason) { %>
                  <div style="margin-top: 0.5rem; font-size: 0.85rem; opacity: 0.8; font-style: italic;">
                    <i class="fas fa-comment-alt"></i> <%= guild.blockReason %>
                  </div>
                <% } %>
              <% } else { %>
                <span class="badge badge-active"><i class="fas fa-check"></i> Aktiv</span>
              <% } %>
            </td>
            <% if (!restrictedView) { %>
            <td>
              <div style="display: flex; gap: 0.5rem; flex-wrap: wrap;">
                <% if (guild.blocked) { %>
                  <button class="btn" style="padding: 0.5rem 1rem; font-size: 0.85rem; background: var(--accent-color); color: #000;" onclick="showUnblockModal('<%= guild.id %>', '<%= guild.name %>')">
                    <i class="fas fa-check-circle"></i> Entsperren
                  </button>
                <% } else { %>
                  <button class="btn" style="padding: 0.5rem 1rem; font-size: 0.85rem; background: #f39c12;" onclick="showBlockModal('<%= guild.id %>', '<%= guild.name %>')">
                    <i class="fas fa-ban"></i> Blockieren
                  </button>
                  <button class="btn" style="padding: 0.5rem 1rem; font-size: 0.85rem; background: #e67e22;" onclick="showKickModal('<%= guild.id %>', '<%= guild.name %>')">
                    <i class="fas fa-door-open"></i> Kicken
                  </button>
                  <button class="btn" style="padding: 0.5rem 1rem; font-size: 0.85rem; background: #e74c3c;" onclick="showForceDeleteModal('<%= guild.id %>', '<%= guild.name %>')">
                    <i class="fas fa-trash-alt"></i> Force Löschen
                  </button>
                <% } %>
              </div>
            </td>
            <% } %>
          </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>

    <!-- Users Management Section -->
    <div class="section" style="margin-top: 3rem;">
      <h2 class="section-title">
        <i class="fas fa-users"></i>
        Nutzer-Verwaltung (<%= users.length %>)
      </h2>

      <div class="search-box">
        <input type="text" id="searchUsersInput" placeholder="🔍 Nutzer suchen (Name oder ID)...">
      </div>

      <div class="table-wrapper">
        <table class="guilds-table" id="usersTable">
          <thead>
            <tr>
              <th>Nutzer</th>
              <th>Erster Login</th>
              <th>Letzter Login</th>
              <th>Logins</th>
              <th>Status</th>
              <% if (!restrictedView) { %>
              <th>Aktionen</th>
              <% } %>
            </tr>
          </thead>
          <tbody>
            <% users.forEach(user => { %>
            <tr>
              <td>
                <div class="guild-info">
                  <% if (user.avatar) { %>
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png?size=64" alt="<%= user.username %>" class="guild-icon" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                    <div class="guild-icon-placeholder" style="display: none;"><%= user.username[0] %></div>
                  <% } else { %>
                    <div class="guild-icon-placeholder"><%= user.username[0] %></div>
                  <% } %>
                  <div>
                    <div class="guild-name"><%= user.username %>#<%= user.discriminator %></div>
                    <div class="guild-id"><%= user.id %></div>
                  </div>
                </div>
              </td>
              <td><%= new Date(user.firstLogin).toLocaleDateString('de-DE') %></td>
              <td><%= new Date(user.lastLogin).toLocaleDateString('de-DE') %></td>
              <td><strong><%= user.loginCount %></strong></td>
              <td>
                <% if (user.blocked) { %>
                  <% if (user.isPermanent) { %>
                    <span class="badge badge-blocked"><i class="fas fa-ban"></i> Permanent gesperrt</span>
                  <% } else if (user.expiresAt) { %>
                    <%
                      const now = Date.now();
                      const expiryTime = new Date(user.expiresAt).getTime();
                      const remaining = expiryTime - now;
                      const daysLeft = Math.ceil(remaining / (1000 * 60 * 60 * 24));
                      const hoursLeft = Math.ceil(remaining / (1000 * 60 * 60));
                    %>
                    <span class="badge" style="background: #f39c12; color: white; border-color: #f39c12;">
                      <i class="fas fa-clock"></i> Temp. gesperrt (<%= daysLeft > 0 ? daysLeft + ' Tag' + (daysLeft === 1 ? '' : 'e') : hoursLeft + 'h' %>)
                    </span>
                    <div style="margin-top: 0.25rem; font-size: 0.75rem; opacity: 0.6;">
                      Läuft ab: <%= new Date(user.expiresAt).toLocaleString('de-DE', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %>
                    </div>
                  <% } else { %>
                    <span class="badge badge-blocked"><i class="fas fa-ban"></i> Gesperrt</span>
                  <% } %>
                  <% if (user.blockReason) { %>
                    <div style="margin-top: 0.5rem; font-size: 0.85rem; opacity: 0.8; font-style: italic;">
                      <i class="fas fa-comment-alt"></i> <%= user.blockReason %>
                    </div>
                  <% } %>
                  <% if (user.blockedBy) { %>
                    <div style="margin-top: 0.25rem; font-size: 0.75rem; opacity: 0.6;">
                      von <%= user.blockedBy %> am <%= new Date(user.blockedAt).toLocaleDateString('de-DE') %>
                    </div>
                  <% } %>
                <% } else { %>
                  <span class="badge badge-active"><i class="fas fa-check"></i> Aktiv</span>
                <% } %>
              </td>
              <% if (!restrictedView) { %>
              <td>
                <div style="display: flex; gap: 0.5rem; flex-wrap: wrap;">
                  <% if (user.blocked) { %>
                    <button class="btn" style="padding: 0.5rem 1rem; font-size: 0.85rem; background: var(--accent-color); color: #000;" onclick="showUnblockUserModal('<%= user.id %>', '<%= user.username %>')">
                      <i class="fas fa-check-circle"></i> Entsperren
                    </button>
                  <% } else { %>
                    <button class="btn" style="padding: 0.5rem 1rem; font-size: 0.85rem; background: #f39c12;" onclick="showBlockUserModal('<%= user.id %>', '<%= user.username %>')">
                      <i class="fas fa-ban"></i> Sperren
                    </button>
                  <% } %>
                </div>
              </td>
              <% } %>
            </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>

    <div style="text-align: center; margin-top: 3rem;">
      <a href="/" class="btn btn-secondary">
        <i class="fas fa-home"></i> Zurück zur Startseite
      </a>
    </div>
  </div>

  <!-- Block Server Modal -->
  <div class="modal-overlay" id="blockModal">
    <div class="modal-content">
      <div class="modal-icon" style="color: #f39c12;">
        <i class="fas fa-ban"></i>
      </div>
      <h3 class="modal-title">Server blockieren?</h3>
      <p class="modal-message">
        Möchtest du <strong id="blockServerName"></strong> wirklich blockieren?
        <br><br>
        <span style="opacity: 0.7; font-size: 0.95rem;">
          Der Bot wird folgendes tun:
          <br>• Ban-Nachricht im Server hinterlassen
          <br>• Server automatisch verlassen
          <br>• Server auf Blacklist setzen
          <br>• Automatisches Verlassen bei erneutem Beitritt
          <br>• Kann später entsperrt werden
        </span>
      </p>
      <div style="margin: 1.5rem 0;">
        <label for="banReason" style="display: block; margin-bottom: 0.5rem; font-weight: 600; color: var(--text-color);">
          <i class="fas fa-comment-alt"></i> Ban-Grund (wird im Server angezeigt):
        </label>
        <textarea
          id="banReason"
          rows="4"
          maxlength="500"
          placeholder="z.B. Verstoß gegen die Bot-Nutzungsbedingungen..."
          style="width: 100%; padding: 0.75rem; border: 1px solid var(--glass-border); border-radius: 8px; background: var(--glass-bg); color: var(--text-color); font-family: inherit; resize: vertical; min-height: 80px;"
        ></textarea>
        <small style="opacity: 0.6; font-size: 0.85rem;">Max. 500 Zeichen</small>
      </div>
      <div class="modal-buttons">
        <button class="modal-btn modal-btn-cancel" onclick="closeBlockModal()">
          <i class="fas fa-times"></i> Abbrechen
        </button>
        <button class="modal-btn modal-btn-confirm" style="background: #f39c12;" onclick="confirmBlock()">
          <i class="fas fa-ban"></i> Blockieren
        </button>
      </div>
    </div>
  </div>

  <!-- Unblock Server Modal -->
  <div class="modal-overlay" id="unblockModal">
    <div class="modal-content">
      <div class="modal-icon" style="color: var(--accent-color);">
        <i class="fas fa-check-circle"></i>
      </div>
      <h3 class="modal-title">Server entsperren?</h3>
      <p class="modal-message">
        Möchtest du <strong id="unblockServerName"></strong> wirklich entsperren?
        <br><br>
        <span style="opacity: 0.7; font-size: 0.95rem;">
          Der Bot kann diesem Server wieder beitreten:
          <br>• Server wird von Blacklist entfernt
          <br>• Normaler Beitritt wieder möglich
        </span>
      </p>
      <div class="modal-buttons">
        <button class="modal-btn modal-btn-cancel" onclick="closeUnblockModal()">
          <i class="fas fa-times"></i> Abbrechen
        </button>
        <button class="modal-btn modal-btn-confirm" style="background: var(--accent-color); color: #000;" onclick="confirmUnblock()">
          <i class="fas fa-check-circle"></i> Entsperren
        </button>
      </div>
    </div>
  </div>

  <!-- Kick Server Modal -->
  <div class="modal-overlay" id="kickModal">
    <div class="modal-content">
      <div class="modal-icon" style="color: #e67e22;">
        <i class="fas fa-door-open"></i>
      </div>
      <h3 class="modal-title">Bot vom Server kicken?</h3>
      <p class="modal-message">
        Möchtest du den Bot wirklich von <strong id="kickServerName"></strong> entfernen?
        <br><br>
        <span style="opacity: 0.7; font-size: 0.95rem;">
          Der Bot wird sofort vom Server entfernt:
          <br>• Bot verlässt den Server
          <br>• Daten bleiben erhalten
          <br>• Kann wieder eingeladen werden
        </span>
      </p>
      <div class="modal-buttons">
        <button class="modal-btn modal-btn-cancel" onclick="closeKickModal()">
          <i class="fas fa-times"></i> Abbrechen
        </button>
        <button class="modal-btn modal-btn-confirm" style="background: #e67e22;" onclick="confirmKick()">
          <i class="fas fa-door-open"></i> Kicken
        </button>
      </div>
    </div>
  </div>

  <!-- Force Delete Server Modal -->
  <div class="modal-overlay" id="forceDeleteModal">
    <div class="modal-content">
      <div class="modal-icon" style="color: #e74c3c;">
        <i class="fas fa-trash-alt"></i>
      </div>
      <h3 class="modal-title">Server Force Löschen?</h3>
      <p class="modal-message">
        Möchtest du <strong id="forceDeleteServerName"></strong> wirklich löschen?
        <br><br>
        <span style="opacity: 0.7; font-size: 0.95rem; color: #e74c3c;">
          <strong>⚠️ WARNUNG: Diese Aktion ist unwiderruflich!</strong>
          <br>• Löscht ALLE Server-Daten (Konfiguration, Tickets, Transcripts)
          <br>• Bot verlässt den Server (falls noch darauf)
          <br>• Alle Einstellungen gehen verloren
          <br>• Kann NICHT rückgängig gemacht werden
        </span>
      </p>
      <div class="modal-buttons">
        <button class="modal-btn modal-btn-cancel" onclick="closeForceDeleteModal()">
          <i class="fas fa-times"></i> Abbrechen
        </button>
        <button class="modal-btn modal-btn-confirm" style="background: #e74c3c;" onclick="confirmForceDelete()">
          <i class="fas fa-trash-alt"></i> Permanent Löschen
        </button>
      </div>
    </div>
  </div>

  <!-- Block User Modal -->
  <div class="modal-overlay" id="blockUserModal">
    <div class="modal-content">
      <div class="modal-icon" style="color: #f39c12;">
        <i class="fas fa-user-slash"></i>
      </div>
      <h3 class="modal-title">Nutzer sperren?</h3>
      <p class="modal-message">
        Möchtest du <strong id="blockUserName"></strong> wirklich sperren?
        <br><br>
        <span style="opacity: 0.7; font-size: 0.95rem;">
          Der Nutzer wird vom Panel-Zugriff ausgeschlossen:
          <br>• Kann sich nicht mehr einloggen
          <br>• Aktive Sessions werden beendet
          <br>• Kann später entsperrt werden
        </span>
      </p>
      <div style="margin: 1.5rem 0;">
        <label for="userBlockDuration" style="display: block; margin-bottom: 0.5rem; font-weight: 600; color: var(--text-color);">
          <i class="fas fa-clock"></i> Sperr-Dauer:
        </label>
        <select
          id="userBlockDuration"
          style="width: 100%; padding: 0.75rem; border: 1px solid var(--glass-border); border-radius: 8px; background: var(--glass-bg); color: var(--text-color); font-family: inherit; cursor: pointer; margin-bottom: 1rem;"
        >
          <option value="permanent">Permanent</option>
          <option value="1">1 Tag</option>
          <option value="3">3 Tage</option>
          <option value="7" selected>7 Tage</option>
          <option value="14">14 Tage</option>
          <option value="30">30 Tage</option>
          <option value="90">90 Tage</option>
        </select>
      </div>
      <div style="margin: 1.5rem 0;">
        <label for="userBlockReason" style="display: block; margin-bottom: 0.5rem; font-weight: 600; color: var(--text-color);">
          <i class="fas fa-comment-alt"></i> Sperr-Grund:
        </label>
        <textarea
          id="userBlockReason"
          rows="4"
          maxlength="500"
          placeholder="z.B. Missbrauch des Panels..."
          style="width: 100%; padding: 0.75rem; border: 1px solid var(--glass-border); border-radius: 8px; background: var(--glass-bg); color: var(--text-color); font-family: inherit; resize: vertical; min-height: 80px;"
        ></textarea>
        <small style="opacity: 0.6; font-size: 0.85rem;">Max. 500 Zeichen</small>
      </div>
      <div class="modal-buttons">
        <button class="modal-btn modal-btn-cancel" onclick="closeBlockUserModal()">
          <i class="fas fa-times"></i> Abbrechen
        </button>
        <button class="modal-btn modal-btn-confirm" style="background: #f39c12;" onclick="confirmBlockUser()">
          <i class="fas fa-user-slash"></i> Sperren
        </button>
      </div>
    </div>
  </div>

  <!-- Unblock User Modal -->
  <div class="modal-overlay" id="unblockUserModal">
    <div class="modal-content">
      <div class="modal-icon" style="color: var(--accent-color);">
        <i class="fas fa-user-check"></i>
      </div>
      <h3 class="modal-title">Nutzer entsperren?</h3>
      <p class="modal-message">
        Möchtest du <strong id="unblockUserName"></strong> wirklich entsperren?
        <br><br>
        <span style="opacity: 0.7; font-size: 0.95rem;">
          Der Nutzer kann sich wieder einloggen:
          <br>• Panel-Zugriff wird wiederhergestellt
          <br>• Normaler Login wieder möglich
        </span>
      </p>
      <div class="modal-buttons">
        <button class="modal-btn modal-btn-cancel" onclick="closeUnblockUserModal()">
          <i class="fas fa-times"></i> Abbrechen
        </button>
        <button class="modal-btn modal-btn-confirm" style="background: var(--accent-color); color: #000;" onclick="confirmUnblockUser()">
          <i class="fas fa-user-check"></i> Entsperren
        </button>
      </div>
    </div>
  </div>

  <script>
    let currentGuildId = null;
    let currentUserId = null;
    let currentGuildName = null;

    // Search functionality
    const searchInput = document.getElementById('searchInput');
    const guildsTable = document.getElementById('guildsTable');
    const rows = Array.from(guildsTable.getElementsByTagName('tr'));

    searchInput.addEventListener('input', (e) => {
      const searchTerm = e.target.value.toLowerCase();
      rows.forEach(row => {
        const text = row.textContent.toLowerCase();
        row.style.display = text.includes(searchTerm) ? '' : 'none';
      });
    });

    // Block Modal
    function showBlockModal(guildId, guildName) {
      currentGuildId = guildId;
      currentGuildName = guildName;
      document.getElementById('blockServerName').textContent = guildName;
      document.getElementById('blockModal').classList.add('active');
    }

    function closeBlockModal() {
      document.getElementById('blockModal').classList.remove('active');
      document.getElementById('banReason').value = ''; // Clear reason field
      currentGuildId = null;
      currentGuildName = null;
    }

    function confirmBlock() {
      if (currentGuildId) {
        const banReason = document.getElementById('banReason').value.trim();

        // Validate ban reason
        if (!banReason || banReason.length === 0) {
          alert('Bitte gib einen Ban-Grund an!');
          return;
        }

        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '/founder/block-server/' + currentGuildId;

        // Add reason field
        const reasonInput = document.createElement('input');
        reasonInput.type = 'hidden';
        reasonInput.name = 'reason';
        reasonInput.value = banReason;
        form.appendChild(reasonInput);

        document.body.appendChild(form);
        form.submit();
      }
    }

    // Unblock Modal
    function showUnblockModal(guildId, guildName) {
      currentGuildId = guildId;
      currentGuildName = guildName;
      document.getElementById('unblockServerName').textContent = guildName;
      document.getElementById('unblockModal').classList.add('active');
    }

    function closeUnblockModal() {
      document.getElementById('unblockModal').classList.remove('active');
      currentGuildId = null;
      currentGuildName = null;
    }

    function confirmUnblock() {
      if (currentGuildId) {
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '/founder/unblock-server/' + currentGuildId;
        document.body.appendChild(form);
        form.submit();
      }
    }

    // Kick Modal
    function showKickModal(guildId, guildName) {
      currentGuildId = guildId;
      currentGuildName = guildName;
      document.getElementById('kickServerName').textContent = guildName;
      document.getElementById('kickModal').classList.add('active');
    }

    function closeKickModal() {
      document.getElementById('kickModal').classList.remove('active');
      currentGuildId = null;
      currentGuildName = null;
    }

    function confirmKick() {
      if (currentGuildId) {
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '/founder/kick-server/' + currentGuildId;
        document.body.appendChild(form);
        form.submit();
      }
    }

    // Force Delete Modal
    function showForceDeleteModal(guildId, guildName) {
      currentGuildId = guildId;
      currentGuildName = guildName;
      document.getElementById('forceDeleteServerName').textContent = guildName;
      document.getElementById('forceDeleteModal').classList.add('active');
    }

    function closeForceDeleteModal() {
      document.getElementById('forceDeleteModal').classList.remove('active');
      currentGuildId = null;
      currentGuildName = null;
    }

    function confirmForceDelete() {
      if (currentGuildId) {
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '/founder/force-delete-server/' + currentGuildId;
        document.body.appendChild(form);
        form.submit();
      }
    }

    // Close modals on overlay click
    document.querySelectorAll('.modal-overlay').forEach(modal => {
      modal.addEventListener('click', function(e) {
        if (e.target === this) {
          closeBlockModal();
          closeUnblockModal();
          closeKickModal();
          closeForceDeleteModal();
        }
      });
    });

    // User Management Functions
    function showBlockUserModal(userId, username) {
      currentUserId = userId;
      document.getElementById('blockUserName').textContent = username;
      document.getElementById('blockUserModal').classList.add('active');
    }

    function closeBlockUserModal() {
      document.getElementById('blockUserModal').classList.remove('active');
      document.getElementById('userBlockReason').value = '';
      currentUserId = null;
    }

    function confirmBlockUser() {
      const reason = document.getElementById('userBlockReason').value.trim();
      const duration = document.getElementById('userBlockDuration').value;
      if (currentUserId) {
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '/founder/block-user/' + currentUserId;

        const reasonInput = document.createElement('input');
        reasonInput.type = 'hidden';
        reasonInput.name = 'reason';
        reasonInput.value = reason || 'Kein Grund angegeben';
        form.appendChild(reasonInput);

        const durationInput = document.createElement('input');
        durationInput.type = 'hidden';
        durationInput.name = 'duration';
        durationInput.value = duration;
        form.appendChild(durationInput);

        document.body.appendChild(form);
        form.submit();
      }
    }

    function showUnblockUserModal(userId, username) {
      currentUserId = userId;
      document.getElementById('unblockUserName').textContent = username;
      document.getElementById('unblockUserModal').classList.add('active');
    }

    function closeUnblockUserModal() {
      document.getElementById('unblockUserModal').classList.remove('active');
      currentUserId = null;
    }

    function confirmUnblockUser() {
      if (currentUserId) {
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = '/founder/unblock-user/' + currentUserId;
        document.body.appendChild(form);
        form.submit();
      }
    }

    // Search functionality for users
    const searchUsersInput = document.getElementById('searchUsersInput');
    const usersTable = document.getElementById('usersTable');

    if (searchUsersInput && usersTable) {
      searchUsersInput.addEventListener('input', function(e) {
        const searchTerm = e.target.value.toLowerCase();
        const rows = usersTable.getElementsByTagName('tbody')[0].getElementsByTagName('tr');

        for (let row of rows) {
          const username = row.cells[0].textContent.toLowerCase();
          const userId = row.cells[0].querySelector('.guild-id').textContent.toLowerCase();

          if (username.includes(searchTerm) || userId.includes(searchTerm)) {
            row.style.display = '';
          } else {
            row.style.display = 'none';
          }
        }
      });
    }

    // Close modals on Escape key
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape') {
        closeBlockModal();
        closeUnblockModal();
        closeKickModal();
        closeForceDeleteModal();
        closeBlockUserModal();
        closeUnblockUserModal();
      }
    });

    // Close modals on overlay click
    document.querySelectorAll('.modal-overlay').forEach(modal => {
      modal.addEventListener('click', function(e) {
        if (e.target === this) {
          closeBlockModal();
          closeUnblockModal();
          closeKickModal();
          closeForceDeleteModal();
          closeBlockUserModal();
          closeUnblockUserModal();
        }
      });
    });
  </script>
  <script src="/js/auth-session.js"></script>
</body>
</html>
